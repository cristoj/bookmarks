# Development Environment Configuration
# This file is used automatically when running `npm run dev`

# App Configuration
VITE_APP_NAME=Bookmarks App (Dev)
VITE_APP_URL=http://localhost:5173

# Development Mode - Enable Firebase Emulators
# Set to 'true' if you want to use local Firebase emulators
# Make sure emulators are running: firebase emulators:start
VITE_USE_FIREBASE_EMULATORS=false

# Google reCAPTCHA site key (public). Replace with your site key from
# Google reCAPTCHA admin or Firebase Console. If left empty, the Firebase
# Console configuration will be used instead.