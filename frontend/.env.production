# Production Environment Configuration
# This file is used automatically when running `npm run build`

# App Configuration
VITE_APP_NAME=Bookmarks App
# VITE_APP_URL should be set in your deployment platform (Vercel, Netlify, etc.)
# or uncomment and set it here:
# VITE_APP_URL=https://your-production-domain.com

# Production Mode - Never use emulators in production
VITE_USE_FIREBASE_EMULATORS=false

